<!DOCTYPE html>
<html>
  <head>
    <%= stylesheet_link_tag    'index', media: 'all', 'data-turbolinks-track' => true %>
  </head>
  <body>
<!-- p id="notice"><%= notice %></p-->
    <h3>Salas Disponíveis</h3>
    
    <h4 id="notice" style="color: green;"><%= notice %></h4>
    
    <% if @rooms.size > 0 %>
      <table>
        <thead>
        </thead>
        <tbody>
          <% @rooms.each do |room| %>
            <tr>
              <td><%= room.name %></td>
              <td><%= link_to 'Entrar', pokers_path(sala: room.id) %></td>
              
              <%if current_user != nil %> 
                <% if room.user == current_user.name  %> 
                  <td><%= link_to 'Editar', edit_room_path(room) %></td>
                  <td><%= link_to 'Excluir', room, method: :delete, data: { confirm: 'Tem certeza?' } %></td>
                <% end %> 
              <% end %>
              
              <% if room.user == nil%>
                <td><%= link_to 'Editar', edit_room_path(room) %></td>
                <td><%= link_to 'Excluir', room, method: :delete, data: { confirm: 'Tem certeza?' } %></td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% end %>
    <br/>
    <br/>
    <br/>
    <%= link_to 'Nova Sala', new_room_path %>
  </body>
</html>



